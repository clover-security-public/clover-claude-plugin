{
  "name": "clover",
  "version": "0.1.80",
  "description": "Protect your coding agent experience — Clover silently threat-models every plan, surfaces missing security requirements, and folds them in before code is written.",
  "author": {
    "name": "Clover Security"
  },
  "userConfig": {
    "server_url": {
      "title": "Clover API URL",
      "description": "Clover API server URL (e.g. https://app.cloversec.io)",
      "type": "string",
      "sensitive": false
    },
    "auth_url": {
      "title": "Auth URL",
      "description": "Frontegg auth URL (e.g. https://clover.frontegg.com)",
      "type": "string",
      "sensitive": false
    },
    "client_id": {
      "title": "Client ID",
      "description": "API client ID (from Clover Settings > API Tokens)",
      "type": "string",
      "sensitive": false
    },
    "client_secret": {
      "title": "Client Secret",
      "description": "API client secret",
      "type": "string",
      "sensitive": true
    },
    "user_email": {
      "title": "Your Email (optional)",
      "description": "Only needed if Claude Code has no account profile (e.g. API-key auth); otherwise Clover uses your Claude account or git identity",
      "type": "string",
      "sensitive": false
    }
  }
}
